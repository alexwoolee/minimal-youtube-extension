# Minimal YouTube

An extension that modifies YouTube's UI to be more minimal.

# Installation

1. Run `git clone https://github.com/alexwoolee/minimal-youtube-extension`
2. Run `cd minimal-youtube-extension`
3. In Chrome, go here [chrome://extensions](chrome://extensions)
4. Top left, click **Load unpacked** and select the `minimal-youtube-extension` folder
